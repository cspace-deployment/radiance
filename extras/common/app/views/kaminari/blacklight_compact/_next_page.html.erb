<%# Link to the "Next" page
  - available local variables
    url:           url to the next page
    current_page:  a page object for the currently displayed page
    num_pages:     total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
-%>
<%= link_to_unless current_page.last?,
  raw(t 'views.pagination_compact.next'),
  with_screen_reader_alert(
    url,
    sr_alert="page #{number_with_delimiter(current_page + 1, {delimiter: ','})} loaded",
    focus_target=['#pagination-compact-next', '#pagination-compact-prev']
  ),
  :id => 'pagination-compact-next',
  :rel => 'next',
  :remote => remote
%>
